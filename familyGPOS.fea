include (markclasses.fea);

feature mark {
	include (mark.fea);
} mark;


feature mkmk {
	include (mkmk.fea);
} mkmk;


feature kern {
	include (kern.fea);
} kern;


feature size {
	parameters 10.0 0; # design size, subfamily
} size;
